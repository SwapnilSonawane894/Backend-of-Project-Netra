# Safest possible requirements - tested versions that work on Render
setuptools>=70.0.0
wheel>=0.41.0

# Core FastAPI (tested working versions)
fastapi==0.85.0
uvicorn==0.18.3
python-multipart==0.0.5

# Auth - older stable versions
PyJWT==2.4.0
passlib[bcrypt]==1.7.4

# Utilities
requests==2.28.2

# Note: These are older but very stable versions that should compile without issues