# Super minimal - no compilation required but with auth support
setuptools>=70.0.0
wheel>=0.41.0

# Absolute basics - pure Python packages only
fastapi==0.88.0
uvicorn==0.20.0
python-multipart==0.0.5

# Auth dependencies (pure Python)
PyJWT==2.6.0
passlib==1.7.4
bcrypt==4.0.1

# Pure Python utilities
requests==2.28.2

# Note: This includes authentication but avoids pydantic compilation issues